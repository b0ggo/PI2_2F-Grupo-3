
# Deixe vazio para usar o proxy do Vite (/api -> backend)
VITE_API_URL=

# Usado pelo proxy do Vite em dev local (padrão: http://localhost:5000)
# No Docker Compose isso é definido automaticamente como http://backend:5000
# VITE_PROXY_TARGET=http://localhost:5000
